openapi: 3.0.0
info:
  title: AI4U API
  version: 1.0

paths:
  /api/v1/ask:
    post:
      summary: Ask a question
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                text: {type: string}
                lang: {type: string}
      responses:
        200: {description: Response}
