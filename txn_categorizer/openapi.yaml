openapi: 3.0.0
info:
  title: Transaction Categorizer API
  version: 1.0
paths:
  /predict:
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                merchant: {type: string}
      responses:
        200:
          description: Category result
